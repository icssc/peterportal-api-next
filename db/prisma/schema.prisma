generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mysql"
  url      = env("DATABASE_URL")
}

model messages {
  idx     BigInt   @unique(map: "idx") @default(autoincrement()) @db.UnsignedBigInt
  stamp   DateTime @db.DateTime(0)
  message String?  @db.Text
}
