{
  "pipeline": {
    "generate": {
      "cache": false
    },
    "ant-stack#build": {},
    "build": {
      "dependsOn": ["ant-stack#build", "^build", "^generate"],
      "outputs": ["dist/**"]
    },
    "deploy": {
      "dependsOn": ["^build"],
      "env": [
        "ACCOUNT_ID",
        "API_PORT",
        "AWS_REGION",
        "BASE_URL",
        "CERTIFICATE_ARN",
        "DATABASE_URL",
        "DATABASE_URL_SCRAPER",
        "HOSTED_ZONE_ID",
        "NODE_ENV",
        "PR_NUM",
        "STAGE",
        "GITHUB_TOKEN",
        "npm_config_user_agent"
      ]
    },
    "destroy": {
      "cache": false
    },
    "dev": {
      "cache": false,
      "dependsOn": ["^generate"],
      "env": ["API_PORT"]
    },
    "test": {}
  }
}
