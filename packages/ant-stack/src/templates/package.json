{
  "name": "$name",
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "build": "pnpm exec ant-stack build",
    "dev": "pnpm exec ant-stack dev",
    "start": "node dist"
  },
  "dependencies": {
    "ant-stack": "workspace:^"
  },
  "devDependencies": {
    "@libs/build-tools": "workspace:*",
    "peterportal-api-next-types": "workspace:*"
  }
}
